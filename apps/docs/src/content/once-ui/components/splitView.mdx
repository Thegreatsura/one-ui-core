---
title: "SplitView"
summary: "A resizable split view component that allows users to adjust the size ratio between two panels."
updatedAt: "2025-04-20"
docs: "once-ui/components/splitView.mdx"
github: "components/SplitView.tsx"
navLabel: "SplitView"
navIcon: "components"
---

The `SplitView` component creates a resizable divider between two panels, allowing users to adjust the size ratio. It supports both horizontal and vertical layouts with customizable resize handles.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  codeHeight={32}
  preview={
    <Row fillWidth>
      <SplitView 
        leftPanel={
          <Column padding="20" background="neutral-medium" radius="l">
            <Text variant="heading-strong-m">Left Panel</Text>
            <Text onBackground="neutral-weak">
              This is the left panel content. You can resize this panel by dragging the divider.
            </Text>
          </Column>
        }
        rightPanel={
          <Column padding="20" background="neutral-weak" radius="l">
            <Text variant="heading-strong-m">Right Panel</Text>
            <Text onBackground="neutral-medium">
              This is the right panel content. The divider is draggable and provides visual feedback.
            </Text>
          </Column>
        }
        defaultSplit={0.4}
        resizeHandleSize="m"
      />
    </Row>
  }
  codes={[
    {
      language: "tsx",
      label: "SplitView",
      code:
`<SplitView 
  leftPanel={
    <Column padding="20" background="neutral-medium" radius="l">
      <Text variant="heading-strong-m">Left Panel</Text>
      <Text onBackground="neutral-weak">
        This is the left panel content.
      </Text>
    </Column>
  }
  rightPanel={
    <Column padding="20" background="neutral-weak" radius="l">
      <Text variant="heading-strong-m">Right Panel</Text>
      <Text onBackground="neutral-medium">
        This is the right panel content.
      </Text>
    </Column>
  }
  defaultSplit={0.4}
  resizeHandleSize="m"
/>`,
    }
  ]}
/>

## Vertical Layout

SplitView also supports vertical orientation for top-bottom layouts.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  codeHeight={32}
  preview={
    <Row fillWidth>
      <SplitView 
        direction="column"
        leftPanel={
          <Column padding="20" background="neutral-medium" radius="l">
            <Text variant="heading-strong-m">Top Panel</Text>
            <Text onBackground="neutral-weak">
              This is the top panel in a vertical split layout.
            </Text>
          </Column>
        }
        rightPanel={
          <Column padding="20" background="neutral-weak" radius="l">
            <Text variant="heading-strong-m">Bottom Panel</Text>
            <Text onBackground="neutral-medium">
              This is the bottom panel. The resize handle is horizontal in vertical mode.
            </Text>
          </Column>
        }
        defaultSplit={0.3}
        resizeHandleSize="s"
      />
    </Row>
  }
  codes={[
    {
      language: "tsx",
      label: "Vertical SplitView",
      code:
`<SplitView 
  direction="column"
  leftPanel={
    <Column padding="20" background="neutral-medium" radius="l">
      <Text variant="heading-strong-m">Top Panel</Text>
    </Column>
  }
  rightPanel={
    <Column padding="20" background="neutral-weak" radius="l">
      <Text variant="heading-strong-m">Bottom Panel</Text>
    </Column>
  }
  defaultSplit={0.3}
  resizeHandleSize="s"
/>`,
    }
  ]}
/>

## Customization

You can customize the resize handle size, appearance, and set minimum/maximum split ratios.

<CodeBlock
  marginTop="16"
  marginBottom="24"
  codeHeight={32}
  preview={
    <Row fillWidth>
      <SplitView 
        leftPanel={
          <Column padding="20" background="brand-weak" radius="l">
            <Text variant="heading-strong-m">Custom Handle</Text>
            <Text onBackground="brand-medium">
              This split view uses a custom handle size and background color.
            </Text>
          </Column>
        }
        rightPanel={
          <Column padding="20" background="neutral-weak" radius="l">
            <Text variant="heading-strong-m">Constrained</Text>
            <Text onBackground="neutral-medium">
              This panel has minimum 20% and maximum 80% split constraints.
            </Text>
          </Column>
        }
        defaultSplit={0.5}
        minSplit={0.2}
        maxSplit={0.8}
        resizeHandleSize="l"
        resizeHandleBackground="brand-medium"
      />
    </Row>
  }
  codes={[
    {
      language: "tsx",
      label: "Customized SplitView",
      code:
`<SplitView 
  leftPanel={
    <Column padding="20" background="brand-weak" radius="l">
      <Text variant="heading-strong-m">Custom Handle</Text>
    </Column>
  }
  rightPanel={
    <Column padding="20" background="neutral-weak" radius="l">
      <Text variant="heading-strong-m">Constrained</Text>
    </Column>
  }
  defaultSplit={0.5}
  minSplit={0.2}
  maxSplit={0.8}
  resizeHandleSize="l"
  resizeHandleBackground="brand-medium"
/>`,
    }
  ]}
/>

## API reference

<PropsTable
  content={[
    ["leftPanel", "ReactNode", "Content for the left/top panel"],
    ["rightPanel", "ReactNode", "Content for the right/bottom panel"],
    ["defaultSplit", "number", "Initial split ratio (0-1, default: 0.3)"],
    ["minSplit", "number", "Minimum split ratio (0-1, default: 0.1)"],
    ["maxSplit", "number", "Maximum split ratio (0-1, default: 0.9)"],
    ["direction", `'row' | 'column'`, "Split direction (default: 'row')"],
    ["resizeHandleSize", `'xs' | 's' | 'm' | 'l'`, "Size of resize handle (default: 's')"],
    ["resizeHandleBackground", "string", "Background color of resize handle (default: 'neutral-weak')"],
    ["className", "string", "Additional CSS classes"],
    ["style", "CSSProperties", "Inline styles"],
    ["...flex", "FlexProps", "All Flex component props are inherited"]
  ]}
/>

## Usage Guidelines

- **Accessibility**: The resize handle is keyboard accessible and provides proper cursor feedback
- **Performance**: Uses efficient event handling with proper cleanup
- **Responsive**: Works well with responsive layouts and breakpoints
- **Constraints**: Set min/max split ratios to prevent panels from becoming too small
- **Visual Feedback**: Handle changes color when being dragged for better UX